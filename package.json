{
  "name": "tsworkspaces",
  "version": "0.1.0",
  "private": true,
  "description": "This is a test project to test building of npm workspaces with Typescript.",
  "scripts": {
    "build": "tsc --build tsconfig.build.json"
  },
  "author": "Will Foxall",
  "license": "ISC",
  "workspaces": [
    "packages/*",
    "app"
  ],
  "devDependencies": {
    "typescript": "^4.5.5"
  }
}
